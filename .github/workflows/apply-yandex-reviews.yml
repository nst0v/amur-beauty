name: Apply Yandex reviews
on:
  push:
    paths:
      - 'scripts/use-yandex-reviews.py'
      - '.github/workflows/apply-yandex-reviews.yml'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/use-yandex-reviews.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git commit -m 'content: replace DIKIDI reviews with Yandex Maps reviews'
          git pull --rebase origin main
          git push origin HEAD:main
